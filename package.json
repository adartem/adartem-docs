{
  "name": "adartem-docs",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "lint": "echo \"No lint defined\"",
    "test": "echo \"No tests defined\"",
    "build": "echo \"Docs build\" && mkdir -p build && echo 'docs dummy' > build/index.html"
  }
}
